% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiverse.R
\name{create_multi2}
\alias{create_multi2}
\title{Create the scenarios for the multiverse}
\usage{
create_multi2(
  formula,
  data,
  focal = NULL,
  nfuns = NULL,
  cfuns = NULL,
  transf.focal = FALSE,
  subset = NULL,
  fit.fun = NULL,
  fit.fun.args = NULL,
  max_scenarios = 10000,
  fit = TRUE,
  keep_failed = TRUE
)
}
\arguments{
\item{formula}{a formula describing the maximal model with bare variables.
Interactions are currently supported only for focal terms.}

\item{data}{the dataset for the model.}

\item{focal}{optional character vector with the name(s) of the focal predictor(s).
Focal predictors are always included in every model.}

\item{nfuns}{functions to be applied to numerical variables. Can be a character
vector, e.g. c("log", "scale"), or a named list, e.g.
list(x = "log", z = c("sqrt", "scale")). If NULL, no numerical
transformations are added.}

\item{cfuns}{same as nfuns, but for non-numerical variables.}

\item{transf.focal}{logical; if TRUE, transformations are also applied to focal
predictors. Can be length 1 or the same length as focal.}

\item{subset}{optional named list of logical vectors defining row subsets.}

\item{fit.fun}{optional model fitting function, such as lm or glm.}

\item{fit.fun.args}{optional list of additional arguments passed to fit.fun.}

\item{max_scenarios}{maximum number of scenarios allowed before stopping.}

\item{fit}{logical; if TRUE and fit.fun is supplied, models are fitted.}

\item{keep_failed}{logical; if TRUE, failed models are kept as NULL with their
errors stored in the output.}
}
\value{
a list with variables, calls, subset, scenarios, and optionally mods.
}
\description{
Create the scenarios for the multiverse
}
\examples{
create_multi(
  formula = Sepal.Width ~ Sepal.Length + Petal.Width + Species,
  focal = "Sepal.Length",
  nfuns = c("log", "scale"),
  data = iris
)
}
